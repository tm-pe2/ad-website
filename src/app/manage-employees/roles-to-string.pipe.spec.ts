import { RolesToStringPipe } from './roles-to-string.pipe';

describe('RolesToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
