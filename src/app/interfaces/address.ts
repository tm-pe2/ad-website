export interface Address{
    adressID: number,
    city: string,
    street: string,
    postalcode: number,
    housNumber: number,
    country: string
}

export interface City{
    zipcode: string,
    name: string,
}