export interface Address{
    adressID: number,
    city: string,
    street: string,
    postalcode: number,
    housNumber: number,
    country: string
}

export interface City{
    postal_code: string,
    city_name: string,
}